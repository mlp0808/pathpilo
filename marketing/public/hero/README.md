# Legacy folder (optional)

Marketing images now live under **`public/images/`** with filenames defined in **`app/config/marketingImages.ts`**.

You can keep exports here for your own workflow; the generator script (`npm run placeholders`) can copy from this folder into `public/images/` when present.

Safe to delete this folder once everything lives in `public/images/`.
