/** Visual tokens for sequential pick UI (map + list). Reuse across features. */

export const SEQUENTIAL_PICK_THEME = {
  /** Idle unpicked pin on map */
  pinIdle: '#334155',
  /** Idle unpicked pin in sidebar */
  pinIdleSidebar: '#1e293b',
  pinIdleIcon: '#94a3b8',
  pinShadow: 'rgba(0,0,0,0.22)',
  /** Picked / confirmed — typically route accent */
  pinPickedText: '#ffffff',
  previewTextOnIdle: '#e2e8f0',
} as const
